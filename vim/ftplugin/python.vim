" Setting for Python
setlocal expandtab " hitting Tab in insert mode will produce the appropriate number of spaces
setlocal shiftwidth=4 " how many columns text is indented with using >> and <<
setlocal softtabstop=4 " with expandtab set, tabs equal this amount of spaces
setlocal smarttab "  When on, a <Tab> in front of a line inserts blanks
