" Setting for Python
set expandtab " hitting Tab in insert mode will produce the appropriate number of spaces
set shiftwidth=4 " how many columns text is indented with using >> and <<
set softtabstop=4 " with expandtab set, tabs equal this amount of spaces
set smarttab "  When on, a <Tab> in front of a line inserts blanks
